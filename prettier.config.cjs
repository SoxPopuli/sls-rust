
/** @type {import("prettier").Config} */
const config = {
  semi: true,
  tabWidth: 2,
  printWidth: 120,
  bracketSpacing: true,
  arrowParens: 'avoid',
  singleQuote: false,
};

module.exports = config;
