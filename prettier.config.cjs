
/** @type {import("prettier").Config} */
const config = {
  semi: false,
  tabWidth: 2,
  printWidth: 120,
  bracketSpacing: true,
  arrowParens: 'avoid',
  singleQuote: true,
};

module.exports = config;
